/* Imports */
// import {addPortDialog} from './src/util/domAdd.js'

/* Const */
const d = document;
const dialog = document.querySelector("#portafolio")

/* Declare Events*/
d.addEventListener("DOMContentLoaded",(e)=>{
    dialog.showModal();
});

d.addEventListener("submit",(e)=>{

})